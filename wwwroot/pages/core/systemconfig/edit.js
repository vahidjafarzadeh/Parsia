'use strict';
let checkboxContainer;
var localVariables = {
    URLs: {
        services: {
            showRow: 'systemConfig/showRow',
            save: 'systemConfig/save'
        }
    }
};

$(document).ready(function () {
    onPageReady();
});